#BSUB -J %E%hwrf%NUM%_output_%CYC%
#BSUB -o /com2/output/%ENVIR%/today/hwrf%NUM%_output_%CYC%.o%J
#BSUB -e /com2/output/%ENVIR%/today/hwrf%NUM%_output_%CYC%.o%J
#BSUB -L /bin/sh
#BSUB -cwd /tmpnwprd
#BSUB -W 00:15
#BSUB -P %PROJ%
#BSUB -q %QUEUESERV2%
#BSUB -R rusage[mem=9000]
#BSUB -R affinity[core]
#BSUB -n 1
#BSUB -R span[ptile=1]


%include <head.h>
%include <envir-p2.h>

set -x

export LSB_PJL_TASK_GEOMETRY="{(0)}"
export MP_EUIDEVICE=sn_all
export MP_EUILIB=us

export job=hwrf%NUM%_output_%CYC%
export PDY=${PDY:-%PDY:%}
export cyc=%CYC%
export storm_num=%NUM%
export stormlabel=storm%NUM%

# versions file for hwrf sets $hwrf_ver and $code_ver
VERSION_FILE=${NWROOT}/versions/hwrf.ver
if [ -f $VERSION_FILE ]; then
  . $VERSION_FILE
else
  ecflow_client --msg="***JOB ${ECF_NAME} ERROR: Version File $VERSION_FILE does not exist ***"
  ecflow_client --abort
  exit
fi

${NWROOT}/hwrf.${hwrf_ver}/jobs/JHWRF_OUTPUT

if [ $? -ne 0 ]; then
  ecflow_client --msg="***JOB ${ECF_NAME} ERROR RUNNING J-SCRIPT ***"
  ecflow_client --abort
  exit
fi

%include <tail.h>
%manual
#
#  PURPOSE:  copy files to $COMOUT directory.
#

# Job specific troubleshooting instructions:
#  see generic troubleshoot manual page
#

# include manual page below
%end
